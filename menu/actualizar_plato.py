import json
import os
import boto3
from decimal import Decimal


def lambda_handle(event, context):
    """
    actualizar_plato (PUT /menu/admin/platos/{platoId})

    Permite modificar los datos de un plato existente 
    (por ejemplo, si el Siu Mai sube de precio en Soles, o si cambia la descripción).
    """
    return 

